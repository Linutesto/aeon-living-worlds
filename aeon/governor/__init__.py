"""The AI world-spirit.

Reads the world only as a compact statistics snapshot (telemetry/stats.py) and
writes to it only as validated directives (directives.py). It never imports the
grids directly, which keeps the spirit honest: it influences pressures, it does not
paint pixels.
"""
