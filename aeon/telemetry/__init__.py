"""Observation layer. Reads the world; never mutates it.

stats.py    a compact snapshot fed to the governor and the dashboard overview.
history.py  the append-only timeline of notable events.
metrics.py  rolling time-series for the dashboard charts.
"""
